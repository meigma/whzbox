// Package session implements the authentication domain: tokens, session
// lifecycle, and the Service use cases. It has no I/O — all external
// interactions are routed through port interfaces defined here.
package session
