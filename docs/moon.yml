language: 'python'
layer: 'application'
stack: 'frontend'
tags:
  - 'docs'
  - 'mkdocs'
  - 'uv'

project:
  title: 'whzbox docs'
  description: 'MkDocs documentation site for whzbox.'
  owner: 'meigma'
  maintainers:
    - 'meigma'

toolchains:
  default: 'system'

fileGroups:
  configs:
    - 'mkdocs.yml'
    - 'pyproject.toml'
    - 'uv.lock'
    - '.python-version'
  sources:
    - 'docs/**/*'

tasks:
  install:
    command: 'uv sync --locked'
    inputs:
      - 'pyproject.toml'
      - 'uv.lock'
      - '.python-version'
    outputs:
      - '.venv/pyvenv.cfg'
    options:
      cache: false

  build:
    command: 'uv run --locked mkdocs build --strict'
    deps:
      - 'docs:install'
      # golangci-lint's formatter scans the workspace. Let it finish before
      # MkDocs replaces build/ so repeated aggregate checks cannot race.
      - 'root:format'
    inputs:
      - '@group(configs)'
      - '@group(sources)'
    outputs:
      - 'build/**/*'

  serve:
    command: 'uv run --locked mkdocs serve --dev-addr 127.0.0.1:8000'
    deps:
      - 'docs:install'
    inputs:
      - '@group(configs)'
      - '@group(sources)'
    options:
      cache: false
