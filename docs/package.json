{
  "name": "whzbox-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Workers Builds compatibility shim for the MkDocs site.",
  "scripts": {
    "build": "python -m pip install --disable-pip-version-check uv==0.11.0 && uv run --locked mkdocs build --strict"
  }
}
